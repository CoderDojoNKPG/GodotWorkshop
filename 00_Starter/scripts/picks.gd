extends Node2D

@onready var w = get_node("/root/Main").w
@onready var h = get_node("/root/Main").h


func create_pick():
	# ATT GÖRA: INSTANSIERA EN GREJ SPELAREN KAN PLOCKA UPP
	# Använd scenen Pick soch slumpa position inom fönstrets storlek
	# Lägg sedan till en timer som skickar en signal till
	# create_pick var 5e sekund,
	pass
	
